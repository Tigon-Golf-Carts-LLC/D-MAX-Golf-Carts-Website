# SEO mission changelog

## 2026-08-24
- Applied nationwide SEO scope for D-MAX Golf Carts.
- Added `seo/keyword-map.md` with primary, secondary, semantic, entity, intent, and topic-cluster mappings.
- Added `seo/audit-report.md` for every public application route.
- Added `api/site-info.json` as a public read-only business and model summary with no inventory.
- Added `.well-known/ai-plugin.json` and `.well-known/agent.json` read-only discovery stubs.
- Updated `llms.txt`, `llms-full.txt`, `geo.txt`, and `locations.json` to state nationwide scope without inventing local data.

## Protected or deliberately skipped
- No DMS inventory feed, inventory routes, vehicle listings, VINs, stock numbers, SRPs, VDPs, pricing logic, or dynamic inventory components were found in this project.
- No inventory files were changed.
- No city/state, address, coordinates, hours, ZIP codes, social profiles, review counts, financing terms, or local landmarks were generated because verified values were not supplied.
- No blog, news, event, or location pages were generated because the site does not currently publish that content.